{
  "_comment": "Required Cloud Run config per service. Read by scripts/verify_cloud_run_config.py during deploy, and by docs/cloud-run-config.md for humans. Names only -- never put secret values in this file.",

  "b2-whatsapp-adapter": {
    "required_env": ["GOOGLE_CLOUD_PROJECT", "VERTEX_LOCATION"],
    "required_secrets": ["WEBHOOK_SECRET"],
    "warn_env": ["GOOGLE_DRIVE_FOLDER_ID"]
  },

  "b2-whatsapp-adapter-staging": {
    "required_env": ["GOOGLE_CLOUD_PROJECT", "VERTEX_LOCATION"],
    "required_secrets": ["WEBHOOK_SECRET"],
    "warn_env": ["GOOGLE_DRIVE_FOLDER_ID"]
  }
}
